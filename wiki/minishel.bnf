
---------------------------------------------------------
<list> ::= <pipeline> {('&&' | '||') <pipeline>}*

<pipeline> ::= <exec> { '|' <exec> }

<exec> ::= <group> 
			| <word> <word>* <redirection>*

<group> ::= '(' <list> ')'

<redirection> ::=  '>' <word>
                |  '<' <word>
                |  '>>' <word>
                |  '<<' <word>

<word> ::= 'a-z' + other symbols
----------------------------------------------------------
